<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#7C34A8"
    tools:context=".registerlogin.LoginActivity">

    <androidx.appcompat.widget.Toolbar
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:background="@color/purple_200"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:id="@+id/toolbar"
        app:title="@string/app_name"
        app:titleTextColor="@color/black"
        />

    <EditText
        android:id="@+id/email_edittext_log"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_margin="50dp"
        android:background="@drawable/edittext_round_shape_register_login"
        android:hint="Email"
        android:inputType="textEmailAddress"
        android:padding="10dp"
        app:layout_constraintHorizontal_bias="0.646"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/toolbar" />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="40dp"
        app:layout_constraintTop_toBottomOf="@+id/email_edittext_log"
        app:layout_constraintLeft_toLeftOf="@+id/email_edittext_log"
        app:layout_constraintRight_toRightOf="@+id/email_edittext_log"
        android:background="@drawable/edittext_round_shape_register_login"
        android:hint="Password"
        android:padding="10dp"
        android:layout_margin="50dp"
        android:inputType="textPassword"
        android:id="@+id/password_edittext_log"
        />
    <Button
        android:id="@+id/login_button"
        android:layout_width="200dp"
        android:layout_height="50dp"
        app:layout_constraintTop_toBottomOf="@+id/password_edittext_log"
        app:layout_constraintRight_toRightOf="@+id/password_edittext_log"
        app:layout_constraintLeft_toLeftOf="@+id/password_edittext_log"
        android:layout_margin="60dp"
        android:text="Login"
        android:textAllCaps="false"
        android:padding="10dp"
        android:background="@drawable/button_round"
        android:textColor="@color/white"
        android:textSize="14sp"
        />
    <TextView
        android:id="@+id/textview_back_register"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@+id/login_button"
        app:layout_constraintLeft_toLeftOf="@+id/login_button"
        app:layout_constraintRight_toRightOf="@+id/login_button"
        android:text="Back to registeration "
        android:textAlignment="center"
        android:layout_margin="20dp"
        android:textColor="@color/white"
        android:fontFamily="sans-serif"
        android:textSize="15sp"
        android:textStyle="bold"

        />
</androidx.constraintlayout.widget.ConstraintLayout>